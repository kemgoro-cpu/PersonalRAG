# PersonalRAG - 依存ライブラリ一覧
# インストール方法:
#   1. PyTorch (CUDA版) を先にインストール:
#      開発機: pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#      本番機: pip install torch==2.11.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#   2. その後:
#      pip install -r requirements.txt

# --- Step 1: 音声 → 文字起こし ---
faster-whisper>=1.0.3
pyannote.audio>=3.1.1
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# --- Step 1-alt: テキスト取り込み（Teams .docx 解析用）---
python-docx>=1.1.0

# --- Step 2: 要約・ToDo抽出 (Ollama 経由) ---
ollama>=0.3.3

# --- Step 3: ベクター検索 ---
chromadb>=0.5.0

# --- Step 4: パイプライン自動化 ---
watchdog>=4.0.0

# --- Step 5: Open WebUI Knowledge 自動同期 ---
requests>=2.31.0

# --- 共通: 設定・環境変数 ---
pyyaml>=6.0.1
python-dotenv>=1.0.1

# --- Step 1-gui: 録音ボタン GUI（任意機能。CLI のみ使うなら不要）---
# グローバルホットキーは Windows 標準 API (RegisterHotKey) を ctypes で直接叩くため
# キー監視用の追加依存は不要。トースト通知のみ winotify を使う（Windows 10/11 専用）。
pystray>=0.19.5
Pillow>=10.0.0
winotify>=1.1.0 ; sys_platform == 'win32'
