# AGENTS.md

このリポジトリで Codex が作業するときの基本ルールです。

詳細手順は `docs/codex/` 配下の文書を参照してください。

## 1. 基本方針

- 最終回答、作業まとめ、PR説明は日本語で書く。
- 作業前に、変更対象と影響範囲を確認する。
- 不明点がある場合でも、調査で分かることは先に調べる。
- 仕様、データ、権限、運用に影響する不明点は、実装前に質問する。
- 関係ないファイルを変更しない。
- 不要な大規模リファクタリングをしない。
- 既存仕様を勝手に変えない。
- 既存の設計、命名、書き方に合わせる。
- セキュリティ、認証、権限、個人情報に関わる変更は慎重に扱う。
- テスト未実行の場合は、未実行であることと理由を明記する。
- 推測で進めた場合は、仮定した内容を明記する。

## 2. 標準フロー

通常の Issue 対応は、以下を参照する。

- `docs/codex/workflows/issue_standard.md`

原則として以下の順で進める。

1. Issue の理解と曖昧点確認
2. 変更対象、影響範囲、既存仕様の調査
3. 必要に応じた `explorer` agent による追加調査
4. 必要に応じた設計確認（通常は Codex 本体で実施し、ユーザーが agent 利用を明示した場合のみ適切な agent を使う）
5. Codex 本体による実装
6. 関連テストの実行
7. 必要に応じた差分レビュー（通常は Codex 本体で実施し、ユーザーが agent 利用を明示した場合のみ適切な agent を使う）
8. 妥当な指摘の修正
9. 日本語で最終まとめと PR 説明を作成

小さく局所的な修正では、毎回 subagent を使う必要はない。変更範囲が広い、影響が読みにくい、レビュー観点を分けた方がよい、またはユーザーが明示した場合だけ使う。

## 3. Issue が曖昧な場合

Issue が曖昧な場合は、以下を参照する。

- `docs/codex/workflows/clarification.md`

特に以下は勝手に決めない。

- 期待動作が複数解釈できる
- 入出力仕様が不明
- UI 仕様が不明
- エラー時の挙動が不明
- 既存仕様を変える可能性がある
- DB、ファイル形式、API 仕様に影響する
- セキュリティ、認証、権限、個人情報に関わる
- 依頼者の意図と実装内容にズレが出そう

## 4. レビュー

差分レビューは以下を参照する。

- `docs/codex/workflows/review_only.md`

レビュー担当はファイルを編集せず、指摘だけ行う。
修正が必要な場合は、Codex 本体が妥当性を判断してから対応する。

## 5. 設計相談

設計判断が必要な場合は、以下を参照する。

- `docs/codex/workflows/design_check.md`

設計確認では過剰設計を避け、小規模な内製チームで運用しやすい案を優先する。

## 6. 実行・テスト

作業後は、可能な範囲で関連テストを実行する。

テストを実行した場合は、以下を明記する。

- 実行したコマンド
- 結果
- 失敗した場合の原因
- 未解決の問題

テストを実行できなかった場合は、理由を明記する。

## 7. 禁止事項

以下は禁止する。

- 関係ないファイルの変更
- 依頼されていない仕様変更
- 不要な大規模リファクタリング
- テストや確認をせずに「問題ありません」と断言すること
- セキュリティ、認証、権限まわりを推測だけで変更すること
- 既存データの破壊につながる操作を確認なしで行うこと

## 8. 最終報告

作業完了時は、日本語で以下をまとめる。

- 対応内容
- 変更したファイル
- テスト結果
- 残リスク
- 人間に確認してほしい点
- PR説明文案
